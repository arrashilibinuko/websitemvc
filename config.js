var path = require('path');

var config = {
    debug: true,
    port: 3030,
    mysql: {
        host: "localhost",
        username: "root",
        password: "Qwerty123",
        database: "eshop"
    }
};

module.exports = config;